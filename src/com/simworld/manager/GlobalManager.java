package com.simworld.manager;

import com.simworld.objects.Neighborhoods;
import com.simworld.runworld.Main;

public class GlobalManager {
    Neighborhoods neighborhoods = new Neighborhoods();
    Main running = new Main();
    public CheckNeighborhoods(Neighborhoods neighborhoods){//check that everything in the neighboohood class is working well
        Integer conidition= 0b10;//2 in regular numbering

        while (running){
            if (neighborhoods == conidition) {
            }
        }
    }
    public
}
